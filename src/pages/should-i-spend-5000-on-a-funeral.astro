---
import Layout from "../layouts/Layout.astro";
import FAQSection from "../components/FAQSection.astro";

const funeralFaqs = [
  {
    question: "Is $5,000 enough for a funeral?",
    answer:
      "$5,000 may be enough for direct cremation, cremation with a modest memorial, or a simple family-hosted service. It may not cover a full traditional burial with cemetery costs, casket, viewing, embalming, flowers, headstone, and reception.",
  },
  {
    question: "Is $5,000 too much to spend on a funeral?",
    answer:
      "$5,000 is not automatically too much. It is usually safer when savings, insurance, prepaid funds, or family help cover the cost. It becomes risky if the bill requires credit card debt, a personal loan, or money needed for rent, food, transportation, or medical bills.",
  },
  {
    question: "What funeral options fit a $5,000 budget?",
    answer:
      "A $5,000 budget may fit direct cremation, cremation with a small memorial, a simple graveside service, a church or home gathering, a basic urn, obituary, limited flowers, and a modest reception. Local prices and family choices matter.",
  },
  {
    question: "Should I use savings for a $5,000 funeral?",
    answer:
      "Using savings can make sense if emergency reserves remain protected afterward. It is riskier if the funeral leaves the household unable to handle housing, groceries, utilities, transportation, medical needs, or another emergency.",
  },
  {
    question: "How can I keep a funeral near $5,000?",
    answer:
      "Ask for itemized pricing, avoid package upgrades, compare direct cremation and simple memorial options, choose a modest urn or casket, host the gathering in a low-cost location, and let family members contribute to specific expenses.",
  },
];

const costCategories = [
  {
    title: "Direct cremation or simple disposition",
    body:
      "This is often the core of a lower-cost funeral plan. It may include basic services, transportation, paperwork, and cremation without a formal viewing.",
  },
  {
    title: "Memorial or gathering",
    body:
      "A $5,000 budget may include a small church, home, graveside, or community gathering if venue, food, flowers, and printed materials stay modest.",
  },
  {
    title: "Urn, casket, or container",
    body:
      "Product choices can swing the budget fast. A basic urn, simple container, or modest casket helps keep the plan from drifting upward.",
  },
  {
    title: "Obituary and printed materials",
    body:
      "Obituary placement, programs, prayer cards, photos, and keepsakes can be meaningful, but they should be sized to the budget.",
  },
  {
    title: "Transportation and preparation",
    body:
      "Transportation, refrigeration, preparation, permits, and staff fees can appear separately, so the family should ask what is included.",
  },
  {
    title: "Reception and family logistics",
    body:
      "Food, travel, lodging, missed work, and family-hosted meals can matter as much as the funeral invoice when judging affordability.",
  },
];

const reduceCostTips = [
  "Ask for an itemized price list before choosing a funeral package.",
  "Compare direct cremation, cremation with memorial, simple burial, and memorial-only options.",
  "Avoid upgrading the urn, casket, flowers, programs, or keepsakes out of guilt.",
  "Hold the gathering at home, church, a community room, or a family member’s house.",
  "Let relatives cover specific costs such as food, flowers, obituary, music, or travel.",
  "Delay optional headstone upgrades, keepsakes, or larger memorial events until cash is available.",
  "Use photos, stories, music, and a shared meal to create meaning without overspending.",
  "Avoid high-interest credit cards unless the payoff plan is already realistic.",
];

const redFlags = [
  "The $5,000 cost still requires high-interest credit card debt.",
  "The funeral would drain savings below the household’s emergency floor.",
  "The family is choosing upgrades because a simple service feels embarrassing.",
  "One person is signing the contract while others only vaguely promise to help.",
  "The quote is not itemized or the family does not understand what is included.",
  "Insurance, estate money, or reimbursement is assumed but not confirmed.",
  "The payment plan would crowd out rent, groceries, transportation, or medical bills.",
  "The family has not compared direct cremation, memorial-only, or smaller gathering options.",
];

const assumptions = [
  "Monthly income means take-home pay for the person or household paying the funeral bill.",
  "Insurance or family help means money that is confirmed or highly realistic for this funeral.",
  "Liquid savings means cash available without missing essential bills or selling long-term assets.",
  "The emergency target is the minimum cushion the household wants left after paying the funeral cost.",
  "The calculator estimates financial pressure, not the emotional worth of the service.",
  "A simple funeral can still be respectful when it reflects the person’s wishes and protects the family.",
  "A score of 0 is allowed when the cost is fully covered or tiny compared with income and savings.",
];

const relatedCalculators = [
  {
    title: "Funeral Calculators",
    href: "/funeral-calculators",
    body: "Compare funeral, cremation, burial, insurance, savings, and payment-plan decisions.",
  },
  {
    title: "$10,000 Funeral Calculator",
    href: "/should-i-spend-10000-on-a-funeral",
    body: "Compare this leaner plan with a more common full-service funeral budget.",
  },
  {
    title: "$15,000 Funeral Calculator",
    href: "/should-i-spend-15000-on-a-funeral",
    body: "See how much pressure rises when the funeral budget triples.",
  },
  {
    title: "Cremation Cost Calculator",
    href: "/cremation-cost-calculator",
    body: "Estimate direct cremation, memorial, urn, obituary, travel, and reception costs.",
  },
  {
    title: "Funeral vs. Cremation Cost Calculator",
    href: "/funeral-vs-cremation-cost-calculator",
    body: "Compare burial, cremation, direct cremation, and memorial options side by side.",
  },
  {
    title: "Use Savings for Funeral Costs?",
    href: "/should-i-use-savings-for-funeral-costs",
    body: "Decide whether using cash is safer than borrowing or waiting for reimbursement.",
  },
  {
    title: "Emergency Savings Guide",
    href: "/how-much-emergency-savings-should-i-have",
    body: "Estimate how much cash should remain after a major family expense.",
  },
  {
    title: "Debt Pressure Calculator",
    href: "/how-much-debt-is-too-much",
    body: "Check whether funeral borrowing would create too much monthly pressure.",
  },
];
---

<Layout
  title="Should I Spend $5,000 on a Funeral?"
  description="Use this $5,000 funeral affordability calculator to decide whether a simple funeral, cremation, or memorial fits savings, insurance, family help, debt, income, and emergency reserves."
>
  <style>
    .container { max-width: 1060px; margin: 0 auto; padding: 80px 24px; }
    .eyebrow { color:#0f766e; text-transform:uppercase; letter-spacing:2px; font-size:.8rem; font-weight:900; margin-bottom:18px; }
    h1 { font-size:clamp(2.65rem,7vw,4.8rem); line-height:.95; margin:0 0 24px; color:#0f172a; max-width:930px; }
    h2 { font-size:clamp(1.8rem,4vw,2.45rem); line-height:1.12; margin:0 0 18px; color:#0f172a; }
    h3 { color:#0f172a; line-height:1.25; margin:0 0 10px; }
    p, li { color:#334155; line-height:1.85; font-size:1.06rem; }
    a { color:#0f766e; font-weight:800; }
    .intro { max-width:860px; font-size:1.14rem; margin-bottom:32px; }
    .hero-grid, .info-grid, .link-grid, .mini-grid { display:grid; gap:20px; }
    .hero-grid { grid-template-columns:minmax(0,1.4fr) minmax(280px,.7fr); margin:34px 0 56px; }
    .info-grid, .link-grid, .mini-grid { grid-template-columns:repeat(auto-fit,minmax(245px,1fr)); margin-top:28px; }
    .summary-card, .calculator-box, .info-card, .link-card, .callout, .mini-card, .table-wrap {
      border:1px solid #dbe4ee; background:#fff; border-radius:24px; box-shadow:0 12px 28px rgba(15,23,42,.04);
    }
    .summary-card, .info-card, .mini-card, .link-card, .callout { padding:24px; }
    .summary-card { background:linear-gradient(180deg,#f8fafc 0%,#fff 100%); }
    .summary-card strong { display:block; color:#0f172a; font-size:1.1rem; margin-bottom:10px; }
    .summary-list { margin:0; padding-left:20px; }
    .summary-list li { margin-bottom:10px; font-size:.98rem; }
    .calculator-box { padding:30px; margin-bottom:64px; }
    .form-grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(230px,1fr)); gap:18px; margin-top:24px; }
    label { display:block; font-weight:900; color:#0f172a; margin-bottom:8px; font-size:.95rem; }
    .hint { display:block; color:#64748b; font-size:.83rem; line-height:1.45; margin-top:5px; }
    input { width:100%; border:1px solid #cbd5e1; border-radius:13px; padding:13px; font-size:1rem; box-sizing:border-box; color:#0f172a; background:#fff; }
    button { margin-top:24px; border:none; border-radius:999px; padding:15px 24px; background:#0f766e; color:#fff; font-weight:900; font-size:1rem; cursor:pointer; }
    button:hover { opacity:.92; }
    .result { margin-top:32px; background:#f8fafc; border:1px solid #dbe4ee; border-radius:24px; padding:30px; display:none; }
    .result.visible { display:block; }
    .score { font-size:clamp(2rem,4vw,2.75rem); font-weight:950; color:#0f172a; margin-bottom:20px; line-height:1.12; }
    .bar { height:16px; background:#dbe4ee; border-radius:999px; overflow:hidden; margin-bottom:24px; }
    .fill { height:100%; width:0%; border-radius:999px; transition:width .45s ease; }
    .fill.safe { background:linear-gradient(90deg,#0f766e 0%,#14b8a6 100%); }
    .fill.caution { background:linear-gradient(90deg,#ca8a04 0%,#facc15 100%); }
    .fill.danger { background:linear-gradient(90deg,#dc2626 0%,#f97316 100%); }
    .result-details { display:grid; grid-template-columns:repeat(auto-fit,minmax(230px,1fr)); gap:14px; margin-top:20px; }
    .result-metric { background:#fff; border:1px solid #e2e8f0; border-radius:18px; padding:16px; }
    .result-metric strong { display:block; color:#0f172a; margin-bottom:6px; }
    .result-metric span { color:#334155; line-height:1.55; font-size:.96rem; }
    .guidance { margin-top:22px; padding-top:20px; border-top:1px solid #dbe4ee; color:#0f172a; font-size:1.08rem; line-height:1.8; }
    .fine-print { color:#64748b; font-size:.9rem; margin-top:18px; line-height:1.6; }
    .content-section { margin-top:76px; }
    .content-section > p { max-width:900px; }
    .numbered-card { position:relative; padding-top:48px; }
    .numbered-card .number { position:absolute; top:20px; left:22px; width:28px; height:28px; border-radius:999px; background:#ccfbf1; color:#115e59; font-weight:950; display:grid; place-items:center; font-size:.86rem; }
    .callout { background:#f8fafc; margin-top:28px; }
    .callout p { margin:0; color:#0f172a; font-size:1.08rem; }
    .warning-box { border-left:5px solid #dc2626; }
    .decision-box { border-left:5px solid #0f766e; }
    .table-wrap { overflow-x:auto; margin-top:26px; padding:0; }
    table { width:100%; border-collapse:collapse; min-width:720px; }
    th, td { text-align:left; padding:16px; border-bottom:1px solid #e2e8f0; vertical-align:top; color:#334155; line-height:1.55; }
    th { color:#0f172a; background:#f8fafc; font-size:.92rem; text-transform:uppercase; letter-spacing:.04em; }
    tr:last-child td { border-bottom:none; }
    .link-card { display:block; text-decoration:none; color:inherit; transition:transform .15s ease, box-shadow .15s ease; }
    .link-card:hover { transform:translateY(-2px); box-shadow:0 16px 34px rgba(15,23,42,.08); }
    .link-card strong { display:block; color:#0f172a; margin-bottom:8px; line-height:1.3; font-size:1.03rem; }
    .link-card span, .info-card p, .mini-card p { display:block; margin:0; color:#334155; line-height:1.7; font-size:.96rem; }
    .check-list, .red-list { list-style:none; padding-left:0; }
    .check-list li, .red-list li { position:relative; padding-left:30px; margin-bottom:10px; }
    .check-list li::before { content:"✓"; position:absolute; left:0; top:0; color:#0f766e; font-weight:950; }
    .red-list li::before { content:"!"; position:absolute; left:0; top:2px; width:18px; height:18px; border-radius:999px; display:grid; place-items:center; background:#fee2e2; color:#b91c1c; font-weight:950; font-size:.8rem; }
    @media (max-width:760px) {
      .container { padding:54px 18px; }
      .hero-grid { grid-template-columns:1fr; }
      .calculator-box { padding:22px; }
    }
  </style>

  <main class="container">
    <div class="eyebrow">Funeral Cost Calculator</div>

    <h1>Should I Spend $5,000 on a Funeral?</h1>

    <p class="intro">
      A $5,000 funeral can be a practical, respectful plan for families trying
      to keep costs controlled. It can also be too much if the household has no
      savings, no confirmed help, and would need high-interest debt. This
      calculator estimates whether a $5,000 funeral, cremation, or memorial
      creates low, moderate, or high financial pressure.
    </p>

    <div class="hero-grid">
      <div class="summary-card">
        <strong>Fast answer</strong>
        <p>
          A $5,000 funeral is often most realistic when the family chooses direct
          cremation, a modest memorial, a simple gathering, or a lean service
          with clear itemized pricing.
        </p>
      </div>

      <div class="summary-card">
        <strong>This page helps you check:</strong>
        <ul class="summary-list">
          <li>whether the out-of-pocket cost is affordable</li>
          <li>how much emergency savings remains</li>
          <li>whether borrowing creates risk</li>
          <li>which costs can be reduced first</li>
        </ul>
      </div>
    </div>

    <section class="calculator-box">
      <h2>$5,000 Funeral Pressure Calculator</h2>

      <p>
        Enter the numbers for the person or household actually paying. Use
        monthly take-home income, not gross income. Only include insurance,
        prepaid funds, or family help that is confirmed or highly realistic.
      </p>

      <div class="form-grid">
        <div>
          <label for="cost">Estimated funeral cost</label>
          <input id="cost" type="number" value="5000" min="0" inputmode="decimal" />
          <span class="hint">Use the full expected bill, including memorial, cremation, burial, and extras.</span>
        </div>

        <div>
          <label for="savings">Current liquid savings</label>
          <input id="savings" type="number" placeholder="Example: 12000" min="0" inputmode="decimal" />
          <span class="hint">Cash available without missing bills or selling long-term assets.</span>
        </div>

        <div>
          <label for="help">Insurance or family help</label>
          <input id="help" type="number" placeholder="Example: 1500" min="0" inputmode="decimal" />
          <span class="hint">Confirmed insurance, prepaid funds, employer benefits, or contributions.</span>
        </div>

        <div>
          <label for="income">Monthly take-home income</label>
          <input id="income" type="number" placeholder="Example: 6000" min="0" inputmode="decimal" />
          <span class="hint">After-tax income for the person or household paying.</span>
        </div>

        <div>
          <label for="debt">Monthly debt payments</label>
          <input id="debt" type="number" placeholder="Example: 700" min="0" inputmode="decimal" />
          <span class="hint">Credit cards, loans, car payments, student loans, and other debt.</span>
        </div>

        <div>
          <label for="emergencyTarget">Minimum savings you want left afterward</label>
          <input id="emergencyTarget" type="number" placeholder="Example: 10000" min="0" inputmode="decimal" />
          <span class="hint">The cash floor you do not want the funeral to break.</span>
        </div>
      </div>

      <button id="calculateButton" type="button">Check Funeral Affordability</button>

      <div class="result" id="result">
        <div class="score" id="scoreText"></div>
        <div class="bar"><div class="fill" id="scoreFill"></div></div>
        <div class="result-details" id="resultDetails"></div>
        <div class="guidance" id="guidance"></div>
        <div class="fine-print">This calculator is educational only and is not financial, legal, tax, estate, insurance, or funeral-planning advice.</div>
      </div>
    </section>

    <section class="content-section">
      <h2>A $5,000 funeral is a lean budget, not a careless one</h2>
      <p>
        A lower funeral budget is not a failure. Many families create a
        meaningful goodbye through a simple cremation, a small church service, a
        home gathering, shared food, photos, music, and stories. Those choices
        can honor the person who died while protecting the people left behind.
      </p>
      <p>
        The risk is assuming $5,000 is automatically safe. If the payer has no
        savings, existing debt, unstable income, or no confirmed help, even a
        modest funeral can create pressure.
      </p>
      <div class="callout decision-box">
        <p>
          The best $5,000 funeral plan is clear, intentional, itemized, and paid
          without damaging the surviving household’s basic stability.
        </p>
      </div>
    </section>

    <section class="content-section" id="MethodologySection">
      <h2>How this $5,000 funeral affordability calculator works</h2>
      <p>
        The calculator estimates a pressure score from 0 to 100. It starts with
        the out-of-pocket cost after insurance, prepaid arrangements, or family
        help. Then it checks savings impact, emergency savings remaining,
        monthly take-home income, and existing debt pressure.
      </p>

      <div class="mini-grid">
        <div class="mini-card numbered-card">
          <div class="number">1</div>
          <h3>Out-of-pocket cost</h3>
          <p>The calculator subtracts confirmed help from the funeral cost.</p>
        </div>
        <div class="mini-card numbered-card">
          <div class="number">2</div>
          <h3>Savings impact</h3>
          <p>It checks whether the funeral drains cash below your emergency target.</p>
        </div>
        <div class="mini-card numbered-card">
          <div class="number">3</div>
          <h3>Income flexibility</h3>
          <p>It compares the uncovered cost with monthly take-home income.</p>
        </div>
        <div class="mini-card numbered-card">
          <div class="number">4</div>
          <h3>Debt pressure</h3>
          <p>Existing debt raises risk because less monthly cash is available.</p>
        </div>
      </div>

      <p>
        There is no artificial minimum pressure. If the funeral is fully covered,
        the score can be 0. If the uncovered cost is tiny compared with income
        and savings, the score can also be 0.
      </p>
    </section>

    <section class="content-section">
      <h2>When spending $5,000 on a funeral can make sense</h2>
      <ul class="check-list">
        <li>The family wants a simple cremation, modest memorial, or small gathering.</li>
        <li>Insurance, prepaid funds, savings, or family help cover most of the bill.</li>
        <li>Emergency savings remain above the household’s minimum comfort level.</li>
        <li>No high-interest debt is needed.</li>
        <li>The quote is itemized and the family understands what is included.</li>
        <li>The service reflects the person’s wishes without unnecessary upgrades.</li>
      </ul>
    </section>

    <section class="content-section" id="KeyCostCategories">
      <h2>Key cost categories in a $5,000 funeral</h2>
      <p>
        A $5,000 budget can work, but it does not leave much room for vague
        pricing. The family should know which pieces are required and which are
        optional.
      </p>

      <div class="info-grid">
        {costCategories.map((category) => (
          <div class="info-card">
            <h3>{category.title}</h3>
            <p>{category.body}</p>
          </div>
        ))}
      </div>
    </section>

    <section class="content-section">
      <h2>What can fit inside a $5,000 funeral budget?</h2>
      <div class="table-wrap">
        <table>
          <thead>
            <tr>
              <th>Option</th>
              <th>Why it may fit</th>
              <th>Watch out for</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>Direct cremation</td>
              <td>Usually simpler and lower-cost than burial or a full funeral home service.</td>
              <td>Memorial, urn, obituary, keepsakes, and reception costs can still add up.</td>
            </tr>
            <tr>
              <td>Cremation with modest memorial</td>
              <td>Allows a meaningful goodbye without the full cost of burial.</td>
              <td>Venue, food, flowers, printed materials, and travel can push the budget higher.</td>
            </tr>
            <tr>
              <td>Simple graveside service</td>
              <td>May reduce facility and reception costs.</td>
              <td>Cemetery, casket, vault, marker, and opening/closing fees can exceed the budget.</td>
            </tr>
            <tr>
              <td>Family-hosted gathering</td>
              <td>Can focus money on food, photos, music, and people instead of formal upgrades.</td>
              <td>The family still needs to budget for disposition, transportation, and paperwork.</td>
            </tr>
          </tbody>
        </table>
      </div>
    </section>

    <section class="content-section" id="ReduceCostTips">
      <h2>Ways to lower funeral costs without making the goodbye feel cheap</h2>
      <p>
        The meaningful parts of a funeral are often personal, not expensive.
        These steps can help keep a $5,000 plan from drifting into a $10,000 or
        $15,000 plan.
      </p>
      <ul class="check-list">
        {reduceCostTips.map((tip) => <li>{tip}</li>)}
      </ul>
    </section>

    <section class="content-section" id="FinancialRedFlags">
      <h2>Financial red flags before agreeing to a $5,000 funeral</h2>
      <p>
        A $5,000 funeral can still be too expensive if the household is already
        stretched. These warning signs mean the family should pause and compare
        simpler options.
      </p>
      <ul class="red-list">
        {redFlags.map((flag) => <li>{flag}</li>)}
      </ul>
      <div class="callout warning-box">
        <p>
          Be careful when a “small” funeral still requires borrowing. The budget
          is only modest if the payment method is safe.
        </p>
      </div>
    </section>

    <section class="content-section" id="CalculatorAssumptions">
      <h2>Calculator assumptions</h2>
      <ul>
        {assumptions.map((assumption) => <li>{assumption}</li>)}
      </ul>
    </section>

    <section class="content-section">
      <h2>Decision guide: what to do with your result</h2>
      <div class="info-grid">
        <div class="info-card">
          <h3>Low pressure</h3>
          <p>Proceed carefully, keep the quote itemized, and avoid unnecessary upgrades.</p>
        </div>
        <div class="info-card">
          <h3>Moderate pressure</h3>
          <p>Compare direct cremation, smaller memorials, family contributions, and delayed optional costs.</p>
        </div>
        <div class="info-card">
          <h3>High pressure</h3>
          <p>Reduce the plan, seek confirmed help, avoid high-interest debt, and protect emergency savings.</p>
        </div>
      </div>
    </section>

    <section class="content-section" id="RelatedCalculators">
      <h2>Related funeral and emergency expense calculators</h2>
      <div class="link-grid">
        {relatedCalculators.map((calculator) => (
          <a class="link-card" href={calculator.href}>
            <strong>{calculator.title}</strong>
            <span>{calculator.body}</span>
          </a>
        ))}
      </div>
    </section>

    <FAQSection title="$5,000 Funeral FAQ" faqs={funeralFaqs} />
  </main>

  <script>
    const clamp = (value, min, max) => Math.min(Math.max(value, min), max);

    const formatCurrency = (value) => {
      const safeValue = Number.isFinite(value) ? value : 0;
      return safeValue.toLocaleString(undefined, {
        style: "currency",
        currency: "USD",
        maximumFractionDigits: 0,
      });
    };

    const getNumber = (id) => {
      const rawValue = document.getElementById(id)?.value;
      const value = Number(rawValue);
      return Number.isFinite(value) && value >= 0 ? value : 0;
    };

    function getPressureLevel(score) {
      if (score <= 30) {
        return {
          label: "Low Pressure",
          stress: "Low pressure",
          colorClass: "safe",
          action: "Proceed while keeping the service itemized and controlled.",
        };
      }

      if (score <= 60) {
        return {
          label: "Moderate Pressure",
          stress: "Moderate pressure",
          colorClass: "caution",
          action: "Compare lower-cost options and protect emergency savings.",
        };
      }

      return {
        label: "High Pressure",
        stress: "High pressure",
        colorClass: "danger",
        action: "Reduce the funeral cost, seek help, or avoid high-interest debt.",
      };
    }

    const button = document.getElementById("calculateButton");
    const result = document.getElementById("result");
    const scoreText = document.getElementById("scoreText");
    const scoreFill = document.getElementById("scoreFill");
    const resultDetails = document.getElementById("resultDetails");
    const guidance = document.getElementById("guidance");

    button?.addEventListener("click", () => {
      const cost = getNumber("cost");
      const savings = getNumber("savings");
      const help = getNumber("help");
      const income = getNumber("income");
      const debt = getNumber("debt");
      const emergencyTarget = getNumber("emergencyTarget");

      result?.classList.add("visible");

      if (cost <= 0) {
        scoreText.textContent = "Add a funeral cost first";
        scoreFill.style.width = "0%";
        scoreFill.className = "fill safe";
        resultDetails.innerHTML = `
          <div class="result-metric">
            <strong>Missing cost</strong>
            <span>Enter a funeral cost so the calculator can estimate pressure.</span>
          </div>
        `;
        guidance.textContent = "";
        return;
      }

      const outOfPocket = Math.max(cost - help, 0);
      const savingsAfter = savings - outOfPocket;
      const savingsImpact = savings > 0 ? (outOfPocket / savings) * 100 : outOfPocket > 0 ? 150 : 0;
      const monthlyIncomeImpact = income > 0 ? (outOfPocket / income) * 100 : outOfPocket > 0 ? 300 : 0;
      const debtPaymentRatio = income > 0 ? (debt / income) * 100 : debt > 0 ? 100 : 0;
      const emergencyTargetRemaining =
        emergencyTarget > 0 ? (savingsAfter / emergencyTarget) * 100 : null;

      const fullyCovered = outOfPocket <= 0;
      const tinyUncoveredCost =
        outOfPocket > 0 &&
        outOfPocket <= Math.max(100, income * 0.02) &&
        savingsAfter >= 0 &&
        debtPaymentRatio <= 10;

      const hugeSavings =
        outOfPocket > 0 &&
        savingsAfter >= Math.max(emergencyTarget, income * 6, outOfPocket * 20);

      const hugeIncome =
        outOfPocket > 0 &&
        income > 0 &&
        monthlyIncomeImpact <= 2 &&
        debtPaymentRatio <= 5 &&
        savingsAfter >= 0;

      let pressureScore = 0;

      if (fullyCovered || tinyUncoveredCost || hugeSavings || hugeIncome) {
        pressureScore = 0;
      } else {
        pressureScore += clamp(savingsImpact * 0.42, 0, 38);
        pressureScore += clamp(monthlyIncomeImpact * 0.3, 0, 30);
        pressureScore += clamp(debtPaymentRatio * 0.34, 0, 20);

        if (outOfPocket > savings) pressureScore += 24;

        if (savingsAfter < 0) {
          pressureScore += 24;
        } else if (emergencyTarget > 0 && savingsAfter < emergencyTarget * 0.5) {
          pressureScore += 16;
        } else if (emergencyTarget > 0 && savingsAfter < emergencyTarget) {
          pressureScore += 9;
        } else if (emergencyTarget > 0 && savingsAfter >= emergencyTarget * 2.5) {
          pressureScore -= 12;
        } else if (income > 0 && savingsAfter >= income * 6) {
          pressureScore -= 10;
        }

        if (monthlyIncomeImpact > 300) pressureScore += 18;
        else if (monthlyIncomeImpact > 200) pressureScore += 12;
        else if (monthlyIncomeImpact > 100) pressureScore += 6;

        if (debtPaymentRatio >= 45) pressureScore += 14;
        else if (debtPaymentRatio >= 30) pressureScore += 9;
        else if (debtPaymentRatio >= 20) pressureScore += 4;

        if (
          outOfPocket > 0 &&
          savingsImpact <= 0.5 &&
          monthlyIncomeImpact <= 5 &&
          savingsAfter >= Math.max(emergencyTarget, 0)
        ) {
          pressureScore = Math.min(pressureScore, 3);
        } else if (
          outOfPocket > 0 &&
          savingsImpact <= 1 &&
          monthlyIncomeImpact <= 10 &&
          savingsAfter >= Math.max(emergencyTarget, income * 3)
        ) {
          pressureScore = Math.min(pressureScore, 8);
        } else if (
          outOfPocket > 0 &&
          savingsImpact <= 3 &&
          monthlyIncomeImpact <= 20 &&
          savingsAfter >= Math.max(emergencyTarget, 0)
        ) {
          pressureScore = Math.min(pressureScore, 15);
        }
      }

      pressureScore = clamp(Math.round(pressureScore), 0, 100);
      const level = getPressureLevel(pressureScore);

      scoreText.textContent = `${level.label} — ${pressureScore}/100`;
      scoreFill.style.width = `${pressureScore}%`;
      scoreFill.className = `fill ${level.colorClass}`;

      resultDetails.innerHTML = `
        <div class="result-metric"><strong>Pressure level</strong><span>${level.stress}</span></div>
        <div class="result-metric"><strong>Out-of-pocket cost</strong><span>${formatCurrency(outOfPocket)} after insurance, prepaid funds, or family help.</span></div>
        <div class="result-metric"><strong>Savings after funeral</strong><span>${formatCurrency(savingsAfter)}</span></div>
        <div class="result-metric"><strong>Savings impact</strong><span>${savings > 0 ? `${savingsImpact.toFixed(1)}% of available liquid savings.` : "No liquid savings entered."}</span></div>
        <div class="result-metric"><strong>Monthly income impact</strong><span>${income > 0 ? `${monthlyIncomeImpact.toFixed(1)}% of one month’s take-home income.` : "No monthly take-home income entered."}</span></div>
        <div class="result-metric"><strong>Debt payment pressure</strong><span>${income > 0 ? `${debtPaymentRatio.toFixed(1)}% of monthly take-home income already goes to debt.` : "Enter income to estimate debt pressure."}</span></div>
        ${
          emergencyTargetRemaining !== null
            ? `<div class="result-metric"><strong>Emergency savings target</strong><span>${emergencyTargetRemaining.toFixed(1)}% of your target would remain after the funeral.</span></div>`
            : ""
        }
      `;

      if (pressureScore === 0) {
        guidance.textContent =
          `This funeral creates essentially no measurable financial pressure based on the numbers provided. Recommended next step: ${level.action}`;
      } else if (pressureScore <= 30) {
        guidance.textContent =
          `This funeral appears manageable based on the numbers provided. Recommended next step: ${level.action}`;
      } else if (pressureScore <= 60) {
        guidance.textContent =
          `This funeral may be workable, but it deserves caution. Recommended next step: ${level.action}`;
      } else {
        guidance.textContent =
          `This funeral could create real financial strain. Recommended next step: ${level.action}`;
      }
    });
  </script>
</Layout>