<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Should I Spend This Much?</title>

    <style>
      :root {
        --bg: #f4f7fb;
        --card: #ffffff;
        --text: #0f172a;
        --muted: #475569;
        --border: #dbe3ee;
        --primary: #0f172a;
        --accent: #0f766e;
      }

      * {
        box-sizing: border-box;
      }

      body {
      .site-header {
  background: white;
  border-bottom: 1px solid var(--border);
  position: sticky;
  top: 0;
  z-index: 100;
}

.nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 0;
}

.logo {
  font-size: 1.4rem;
  font-weight: 800;
  text-decoration: none;
  color: var(--primary);
  letter-spacing: -0.5px;
}

nav {
  display: flex;
  gap: 24px;
}

nav a {
  text-decoration: none;
  color: var(--muted);
  font-weight: 600;
}

nav a:hover {
  color: var(--primary);
}
        margin: 0;
        font-family: Arial, Helvetica, sans-serif;
        background: var(--bg);
        color: var(--text);
      }

      .container {
        width: 92%;
        max-width: 1200px;
        margin: 0 auto;
      }

      .hero {
        padding: 80px 0 50px;
        text-align: center;
      }

      .eyebrow {
        color: var(--accent);
        font-size: 0.85rem;
        font-weight: 700;
        letter-spacing: 2px;
        text-transform: uppercase;
        margin-bottom: 12px;
      }

      h1 {
        font-size: clamp(2.8rem, 7vw, 5rem);
        line-height: 1;
        margin: 0 0 24px;
        color: var(--primary);
      }

      .hero p {
        max-width: 760px;
        margin: 0 auto;
        font-size: 1.2rem;
        line-height: 1.7;
        color: var(--muted);
      }

      .section {
        margin-top: 70px;
      }

      .section-title {
        font-size: 2rem;
        margin-bottom: 24px;
      }

      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 24px;
      }

      .card {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: 22px;
        padding: 28px;
        transition: 0.2s ease;
      }

      .card:hover {
        transform: translateY(-3px);
      }

      .card-category {
        color: var(--accent);
        font-size: 0.8rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 10px;
      }

      .card h3 {
        margin: 0 0 14px;
        font-size: 1.6rem;
        line-height: 1.2;
      }

      .card p {
        color: var(--muted);
        line-height: 1.6;
        margin-bottom: 24px;
      }

      .button {
        display: inline-block;
        background: var(--primary);
        color: white;
        padding: 14px 22px;
        border-radius: 12px;
        text-decoration: none;
        font-weight: 700;
      }

      .coming-soon {
        opacity: 0.7;
      }

      footer {
        text-align: center;
        padding: 80px 0 40px;
        color: var(--muted);
        font-size: 0.95rem;
      }
.footer {
  border-top: 1px solid var(--border);
  margin-top: 80px;
  padding: 40px 24px;
  text-align: center;
  color: var(--muted);
}

.footer-links {
  display: flex;
  justify-content: center;
  gap: 24px;
  margin-bottom: 18px;
  flex-wrap: wrap;
}

.footer-links a {
  color: var(--text);
  text-decoration: none;
  font-weight: 600;
  font-size: 0.95rem;
}

.footer-links a:hover {
  color: var(--accent);
}
      @media (max-width: 700px) {
        .hero {
          padding-top: 60px;
        }

        .hero p {
          font-size: 1rem;
        }

        .card h3 {
          font-size: 1.35rem;
        }
      }
    </style>
  </head>

  <body>

  <header class="site-header">
    <div class="container nav">
      <a href="/" class="logo">ShouldISpend</a>

      <nav>
        <a href="/">Home</a>
        <a href="/calculators">Calculators</a>
      </nav>
    </div>
  </header>

  <div class="container">
      <section class="hero">
        <div class="eyebrow">Financial Decision Engine</div>

        <h1>Should I Spend This Much?</h1>

        <p>
          Get a fast, practical verdict on whether a purchase, subscription,
          wedding, trip, lifestyle expense, or major payment is financially
          worth it.
        </p>
      </section>

      <section class="section" id="calculators">
        <h2 class="section-title">Start With a Calculator</h2>

        <div class="grid">
          <div class="card">
            <div class="card-category">Weddings</div>

            <h3>Should I Spend $30,000 on a Wedding?</h3>

            <p>
              Analyze wedding affordability based on income, savings, debt,
              financial stress, and long-term flexibility.
            </p>

            <a
              class="button"
              href="/should-i-spend-30000-on-a-wedding"
            >
              Open Calculator
            </a>
          </div>

          <div class="card">
            <div class="card-category">Cars</div>

            <h3>Should I Spend $700 on a Car Payment?</h3>

            <p>
              Evaluate whether your monthly vehicle payment is realistic for
              your income, debt load, and savings cushion.
            </p>

            <a
              class="button"
              href="/should-i-spend-700-on-a-car-payment"
            >
              Open Calculator
            </a>
            </div>
          <div class="card">
  <div class="card-category">Travel</div>

  <h3>Should I Spend $5,000 on a Vacation?</h3>

  <p>
    Evaluate whether a large vacation expense fits your income, savings,
    debt load, and long-term flexibility.
  </p>

  <a href="/should-i-spend-5000-on-a-vacation" class="button">
    Open Calculator
  </a>
</div>
        </div>
      </section>

      <section class="section">
        <h2 class="section-title">Upcoming Categories</h2>

        <div class="grid">
          <div class="card coming-soon">
            <div class="card-category">Housing</div>

            <h3>Mortgage & Rent Calculators</h3>

            <p>
              Tools for rent affordability, mortgage sizing, home upgrades, and
              real estate decisions.
            </p>
          </div>

          <div class="card coming-soon">
            <div class="card-category">Travel</div>

            <h3>Vacation & Flight Spending</h3>

            <p>
              Decide whether luxury trips, flights, resorts, and travel budgets
              make financial sense.
            </p>
          </div>

          <div class="card coming-soon">
            <div class="card-category">Lifestyle</div>

            <h3>Subscriptions & Daily Spending</h3>

            <p>
              Analyze recurring costs, gym memberships, hobbies, entertainment,
              and lifestyle creep.
            </p>
          </div>
        </div>
      </section>

      
      </footer>
    </div>
    <footer class="footer">
  <div class="footer-links">
    <a href="/about">About</a>
    <a href="/privacy-policy">Privacy Policy</a>
    <a href="/disclaimer">Disclaimer</a>
  </div>

  <p>
    ShouldISpend © 2026 — Practical financial decision tools and calculators.
  </p>
</footer>
  </body>
</html>