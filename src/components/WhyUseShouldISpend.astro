<section class="why-use">
  <div class="container">
    <p class="eyebrow">WHY USE SHOULDISPEND</p>

    <h2>Built for Real Spending Decisions</h2>

    <p class="intro">
      ShouldISpend is designed to help users understand the financial tradeoffs
      behind major purchases, not just calculate a number.
    </p>

    <div class="why-grid">
      <div class="why-card">
        <h3>Financial Context</h3>
        <p>
          The calculators consider savings pressure, debt load, income impact,
          emergency cushion, and overall flexibility.
        </p>
      </div>

      <div class="why-card">
        <h3>Plain-English Verdicts</h3>
        <p>
          Each tool explains what the numbers mean in practical terms, so users
          can understand the risk before spending.
        </p>
      </div>

      <div class="why-card">
        <h3>Real-World Categories</h3>
        <p>
          The site focuses on decisions people actually face, including rent,
          weddings, travel, cars, debt, groceries, and major life costs.
        </p>
      </div>

      <div class="why-card">
        <h3>Conservative by Design</h3>
        <p>
          ShouldISpend generally favors financial breathing room over aggressive
          assumptions that leave little margin for surprise expenses.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .why-use {
    padding: 80px 20px;
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
  }

  .eyebrow {
    color: #0f766e;
    font-size: 0.8rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    margin-bottom: 1rem;
  }

  h2 {
    font-size: clamp(2rem, 5vw, 3.25rem);
    line-height: 1.1;
    margin-bottom: 1rem;
    color: #061126;
  }

  .intro {
    max-width: 720px;
    color: #475569;
    line-height: 1.8;
    margin-bottom: 2.5rem;
  }

  .why-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
  }

  .why-card {
    background: white;
    border: 1px solid #d8e0ea;
    border-radius: 18px;
    padding: 1.5rem;
  }

  .why-card h3 {
    margin-top: 0;
    margin-bottom: 0.75rem;
    color: #061126;
  }

  .why-card p {
    color: #475569;
    line-height: 1.7;
    margin: 0;
  }

  @media (max-width: 800px) {
    .why-grid {
      grid-template-columns: 1fr;
    }
  }
</style>