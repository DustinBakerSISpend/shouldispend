<a class="hub-calculator-jump" href="#calculators">
  <span>Browse Calculators</span>
  <span class="hub-calculator-jump__arrow" aria-hidden="true">&#8595;</span>
</a>

<style>
  .hub-calculator-jump {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.55rem;
    min-height: 52px;
    margin: 20px 0 8px;
    padding: 14px 22px;
    border: 2px solid var(--text);
    border-radius: 999px;
    background: var(--text);
    color: #fff;
    font: inherit;
    font-weight: 800;
    line-height: 1.1;
    text-decoration: none;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.10);
    transition:
      transform 160ms ease,
      box-shadow 160ms ease,
      opacity 160ms ease;
  }

  .hub-calculator-jump:hover {
    transform: translateY(-1px);
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.14);
    opacity: 0.94;
  }

  .hub-calculator-jump:focus-visible {
    outline: 3px solid currentColor;
    outline-offset: 4px;
  }

  .hub-calculator-jump__arrow {
    font-size: 1.15em;
    line-height: 1;
  }

  @media (max-width: 640px) {
    .hub-calculator-jump {
      width: 100%;
      min-height: 56px;
      padding-inline: 18px;
    }
  }
</style>
